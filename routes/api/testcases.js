const express = require("express");
const router = express.Router();

// @route GET api/:projectId/testcases
// @desc GET all Test Cases by projectId
// @access all
router.get("/all", (req, res) => {});

module.exports = router;
